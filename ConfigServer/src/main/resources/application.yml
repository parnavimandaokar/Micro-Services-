server:
  port: 8085

Spring:
  application:
    name: CONFIG-SERVER

  cloud:
    config:
      server:
        git:
          uri: https://github.com/parnavimandaokar/microservice-micro-config
          clone-on-start: true